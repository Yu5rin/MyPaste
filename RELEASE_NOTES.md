# アタイの貼り付け v1.2.1

Excel 使用時に **`Ctrl+B` を `Ctrl+Shift+V`（値貼り付け）にリマップ**する Windows 常駐アプリです。

## v1.2.0 からの変更点

### 起動のたびに更新を確認するようにしました
これまでは起動時の確認を 1 日 1 回までに制限していましたが、**起動のたびに確認する**よう変更しました。

確認の間隔は `settings.json` の `check_interval_hours` で変えられます。

```jsonc
{
  "update": {
    "check_on_startup": true,   // false にすると起動時の確認をやめ、手動のみになる
    "check_interval_hours": 0,  // 0 は起動のたびに確認。24 にすると 1 日 1 回までになる
    ...
  }
}
```

通信は従来どおり別スレッドで行うため、**起動やキー操作を妨げません**。失敗しても
「確認できなかった」で済ませ、アプリの動作には影響しません。

### 修正
- 更新ファイルの後始末を確実にしました。ダウンロードした一時ファイルは
  入れ替えの成否にかかわらず削除しますが、書き出しが途中で失敗した場合に
  中途半端なファイルが残ることがありました。あわせて、強制終了などで残った
  場合に備え、起動時に一時フォルダの残骸（1 時間以上前のもの）も掃除します。
  削除対象は `Atai-paste-<バージョン>.exe` という名前のものだけで、本体の
  実行ファイルや無関係なファイルには触れません。
- Windows 以外のホストからクロスビルドする際、リソースコンパイラが無い環境で
  ビルドが失敗する問題を修正しました。Windows 上でのビルドには影響しません。

## 通信するのは 2 つの場合だけ
このアプリが外部と通信するのは、次のときだけです。それ以外では一切通信しません。

| きっかけ | 内容 |
| --- | --- |
| メニュー「更新を確認」を押したとき | 最新リリースを問い合わせる |
| 起動時（**毎回**） | 最新リリースを問い合わせる |

**通信先は `settings.json` に書かれており、確認・変更できます。**
`check_on_startup` を `false` にすれば、起動時の確認をやめて手動のみにできます。

## 主な機能
- **Excel 限定リマップ**: フォアグラウンドが Excel（`EXCEL.EXE`）のときだけ `Ctrl+B` → `Ctrl+Shift+V`。それ以外では `Ctrl+B` はそのまま通します。
- **分かりやすいメニュー**: 有効な項目には「✔」が付きます。
- **自動起動設定**: メニュー「自動起動」でスタートアップ登録／解除。
- **更新の確認と適用**: ダウンロードしたファイルは SHA256 で検証してから適用します。
  実行中の EXE はリネーム方式で入れ替え、失敗した場合は巻き戻します。
- **オートリピート抑制**: `Ctrl+B` を押しっぱなしにしても貼り付けは 1 回だけ。
- 単体 EXE・ポータブル・管理者権限不要。

## 動作環境
- Windows 10 / 11（64bit）

## 使い方
1. 下の **Assets** から `Atai-paste.exe` をダウンロード。
2. 任意のフォルダに置いてダブルクリックで起動（インストール不要）。
3. Excel を最前面にして `Ctrl+B` → 値貼り付けが実行されます。
4. トレイアイコンを右クリック →「Ctrl+B で値貼り付け」「自動起動」「更新を確認」「終了」。

## 既知の制限
- ON/OFF は右クリックメニューから行います（左クリックのトグルは採用ライブラリ `tray-item` の制約で非対応）。
- 他ソフトとのキー競合は対象外です。

## 注意
- 本 EXE はコード署名していないため、初回起動時に Windows SmartScreen の警告が出る場合があります。「詳細情報」→「実行」で起動できます。
