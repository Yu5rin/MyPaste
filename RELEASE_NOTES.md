# アタイの貼り付け v1.2.1

Excel 使用時に **`Ctrl+B` を `Ctrl+Shift+V`（値貼り付け）にリマップ**する Windows 常駐アプリです。

## v1.2.0 からの変更点

### 起動のたびに更新を確認するようにしました
これまでは起動時の確認を 1 日 1 回までに制限していましたが、**起動のたびに確認する**よう変更しました。

確認の間隔は `settings.json` の `check_interval_hours` で変えられます。

```jsonc
{
  "update": {
    "check_on_startup": true,   // false にすると起動時の確認をやめ、手動のみになる
    "check_interval_hours": 0,  // 0 は起動のたびに確認。24 にすると 1 日 1 回までになる
    ...
  }
}
```

通信は従来どおり別スレッドで行うため、**起動やキー操作を妨げません**。失敗しても
「確認できなかった」で済ませ、アプリの動作には影響しません。

### 見やすさの改善
- **ON アイコンの白い縁取りを太くしました。** 従来は縁取りが細く、既定が黒い
  Windows 11 のタスクバーでは黒い「B」が背景に溶けてほとんど見えていませんでした。
- **トレイアイコンにカーソルを合わせると現在のバージョンが分かるようになりました。**
  更新を確認しなくてもバージョンを確認できます。

### 安定性の修正
- **更新処理の多重実行を防止しました。** 起動時の自動確認とメニューからの手動確認が
  同時に走ると、確認ダイアログが二重に出て、両方で「はい」を押した場合に実行ファイルの
  入れ替えが二重に起きる可能性がありました。
- **更新の入れ替え中に中断されても復旧できるようにしました。** 新しい実行ファイルを
  先に配置しておき、入れ替え自体はリネーム 2 回だけで行うよう変更し、中断され得る
  時間を短くしました。あわせて、次回起動時に実行ファイルが失われていれば復元を試みます。
  トレイメニューの「終了」は、入れ替え中であれば完了を待ってから終了します。
- **起動に失敗したときに理由が分かるようになりました。** キーボードフックの設置や
  トレイの初期化に失敗した場合、従来は無言で終了していたため、起動しても何も起きない
  ように見えていました。
- **`settings.json` の設定が無視される場合があったのを修正しました。** メモ帳などで
  保存すると付く UTF-8 の BOM が原因で設定が読めず、`check_on_startup` を `false` に
  しても通信が止まらないことがありました。また、読み込みに失敗した際に既定値で
  上書き保存して編集内容を失う恐れがあったのも直しています。
- ダウンロードが途中で切れた場合に検知できるようにしました（受信量の突き合わせと、
  実行ファイルであることの確認を追加）。
- 更新ファイルの後始末を確実にしました。書き出しが途中で失敗した場合に中途半端な
  ファイルが残らないようにし、起動時に一時フォルダの古い残骸も掃除します。

### メッセージの改善
- エラーの文面を分かりやすくしました。先頭に平易な一文を置き、技術的な詳細はその下に
  添える形に統一しています。SHA256 の検証に失敗した際に 64 桁の英数字を表示していたのを
  やめ、「壊れている可能性があります」という表現に変えました。
- 更新の確認や取得に失敗した場合も、リリースページの案内を表示するようにしました。

### その他
- Windows 以外のホストからクロスビルドする際、リソースコンパイラが無い環境で
  ビルドが失敗する問題を修正しました。Windows 上でのビルドには影響しません。

## 通信するのは 2 つの場合だけ
このアプリが外部と通信するのは、次のときだけです。それ以外では一切通信しません。

| きっかけ | 内容 |
| --- | --- |
| メニュー「更新を確認」を押したとき | 最新リリースを問い合わせる |
| 起動時（**毎回**） | 最新リリースを問い合わせる |

**通信先は `settings.json` に書かれており、確認・変更できます。**
`check_on_startup` を `false` にすれば、起動時の確認をやめて手動のみにできます。

## 主な機能
- **Excel 限定リマップ**: フォアグラウンドが Excel（`EXCEL.EXE`）のときだけ `Ctrl+B` → `Ctrl+Shift+V`。それ以外では `Ctrl+B` はそのまま通します。
- **分かりやすいメニュー**: 有効な項目には「✔」が付きます。
- **自動起動設定**: メニュー「自動起動」でスタートアップ登録／解除。
- **更新の確認と適用**: ダウンロードしたファイルは SHA256 で検証してから適用します。
  実行中の EXE はリネーム方式で入れ替え、失敗した場合は巻き戻します。
- **オートリピート抑制**: `Ctrl+B` を押しっぱなしにしても貼り付けは 1 回だけ。
- 単体 EXE・ポータブル・管理者権限不要。

## 動作環境
- Windows 10 / 11（64bit）
- Excel は **Microsoft 365 バージョン 2308（2023年8月）以降**が必須です。
  `Ctrl+Shift+V`（値貼り付け）のショートカットは、それ以前の Excel（**Excel 2019 / 2021
  などの永続版や、古い Microsoft 365**）には割り当てられていません。
  対応していない Excel で ON にすると、`Ctrl+B` が握りつぶされるだけで値貼り付けは
  実行されず、**太字も値貼り付けもできなくなります**。該当する場合はトレイメニューで
  OFF にするか終了してください。

## 使い方
1. 下の **Assets** から `Atai-paste.exe` をダウンロード。
2. 任意のフォルダに置いてダブルクリックで起動（インストール不要）。
3. Excel を最前面にして `Ctrl+B` → 値貼り付けが実行されます。
4. トレイアイコンを右クリック →「Ctrl+B で値貼り付け」「自動起動」「更新を確認」「終了」。

## 既知の制限
- ON/OFF は右クリックメニューから行います（左クリックのトグルは採用ライブラリ `tray-item` の制約で非対応）。
- 他ソフトとのキー競合は対象外です。

## 注意
- 本 EXE はコード署名していないため、初回起動時に Windows SmartScreen の警告が出る場合があります。「詳細情報」→「実行」で起動できます。
