# アタイの貼り付け v1.2.3

Excel 使用時に **`Ctrl+B` を `Ctrl+Shift+V`（値貼り付け）にリマップ**する Windows 常駐アプリです。

**自動更新が「HTTP 403」で失敗する問題を修正しました。** v1.2.2 以前をお使いの方は、
お手数ですが下の Assets から手動でダウンロードして置き換えてください。この版以降は
自動更新が正しく動きます。

## v1.2.2 からの変更点

### 自動更新の 403 エラーを修正
更新を確認すると「詳細: サーバーが HTTP 403 を返しました」と表示されて失敗する問題がありました。

原因は **GitHub API の利用回数の上限**です。この上限は 1 時間あたり 60 回で、
**端末ごとではなく接続元のネットワーク（IP アドレス）ごと**に数えられます。
そのため会社などの共有回線では、他の人の通信で先に使い切られてしまい、
自分が何度も押していなくても 403 になることがありました。

対策として、確認の手順を次のように変えました。

1. まず **Atom フィード**で最新のバージョンだけを調べます。これは上記の上限とは
   **別枠**のため、回数を気にせず使えます。
2. ここで「最新版を使っている」と分かれば、**API を呼ばずに終わります**。
   普段の確認で上限を消費しなくなります。
3. 新しい版が見つかったときだけ API を呼び、ダウンロード先と SHA256 を取得します。
4. その API が失敗しても、ダウンロード先は規則から組み立てられるため**更新を続行**します。

### エラーメッセージの改善
403 のときは、利用者の操作が原因ではないことが伝わる文面に変えました。

> 配布元への問い合わせが、回数の上限に達していました。この上限は同じネットワークを使う人たちで
> 共有されるため、自分が何度も押していなくても起こります。しばらく時間をおくか、
> リリースページから直接ご確認ください。

### バージョンを確認しやすくしました
**トレイメニューの先頭に、現在のバージョンを表示**するようにしました。

```
アタイの貼り付け v1.2.3
─────────────────
✔ Ctrl+B で値貼り付け
✔ 自動起動
─────────────────
更新を確認
─────────────────
終了
```

v1.2.2 ではツールチップに入れていましたが、カーソルを合わせないと見えず、
Windows 11 ではトレイアイコン自体が「^」の中に隠れるため、実質的に確認できませんでした。

### 安全性と応答性
- **タイムアウトを明示**しました（確認 15 秒 / ダウンロード 10 分）。従来は既定値まかせで、
  応答が無い場合に環境によっては最大 90 秒ほど無反応になり得ました。
- **更新ファイルの取得先を GitHub のホストに限定**しました。実行ファイルを取得して
  そのまま実行する処理のため、応答に書かれた URL をそのまま信用しません。

### SHA256 の検証についての訂正
README に「必ず SHA256 で検証します」と書いていましたが、**これは正確ではありませんでした**。
API から `digest` を取得できたときだけ検証しており、取得できない場合（上記の 403 など）は
省略しています。実態に合わせて記述を訂正しました。

省略した場合も通信は HTTPS で行っているため、そこで防げるのは転送中の破損までです。
あわせて、取得先ホストの限定と、先頭 2 バイトが `MZ`（実行ファイルの署名）であることの
確認は省略時も行っています。

## 動作環境
- Windows 10 / 11（64bit）
- Excel は **Microsoft 365 バージョン 2308（2023年8月）以降**が必須です。

> **重要: お使いの Excel をご確認ください**
> `Ctrl+Shift+V`（値貼り付け）のショートカットは、**Microsoft 365 のバージョン 2308
> （2023年8月）以降**で追加されたものです。**Excel 2019 / 2021（永続版）や、それより
> 古い Microsoft 365 では割り当てられていません。**
>
> 対応していない Excel でこのアプリを ON にすると、`Ctrl+B` は握りつぶされるものの
> Excel が `Ctrl+Shift+V` を認識しないため、**太字にすることも値貼り付けすることも
> できなくなります**。「`Ctrl+B` が壊れた」ように見えますが、原因はこのアプリによる
> リマップです。該当する場合は、トレイメニューでこのアプリを OFF にするか終了してください。

## 主な機能
- **Excel 限定リマップ**: フォアグラウンドが Excel（`EXCEL.EXE`）のときだけ `Ctrl+B` → `Ctrl+Shift+V`。それ以外では `Ctrl+B` はそのまま通します。
- **分かりやすいメニュー**: 有効な項目には「✔」が付きます。
- **自動起動設定**: メニュー「自動起動」でスタートアップ登録／解除。
- **オートリピート抑制**: `Ctrl+B` を押しっぱなしにしても貼り付けは 1 回だけ。
- 単体 EXE・ポータブル・管理者権限不要。

## 使い方
1. 下の **Assets** から `Atai-paste.exe` をダウンロード。
2. 任意のフォルダに置いてダブルクリックで起動（インストール不要）。
3. Excel を最前面にして `Ctrl+B` → 値貼り付けが実行されます。
4. トレイアイコンを右クリック →「Ctrl+B で値貼り付け」「自動起動」「更新を確認」「終了」。

## 既知の制限
- ON/OFF は右クリックメニューから行います（左クリックのトグルは採用ライブラリ `tray-item` の制約で非対応）。
- 他ソフトとのキー競合は対象外です。

## 注意
- 本 EXE はコード署名していないため、初回起動時に Windows SmartScreen の警告が出る場合があります。「詳細情報」→「実行」で起動できます。
