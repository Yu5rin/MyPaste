## 変更点

### v1.2.2 以前からの更新について
**自動更新が「HTTP 403」で失敗する問題を修正しました。** v1.2.2 以前をお使いの方は、
お手数ですが下の Assets から手動でダウンロードして置き換えてください。この版以降は
自動更新が正しく動きます。

### 自動更新の 403 エラーを修正
更新を確認すると「詳細: サーバーが HTTP 403 を返しました」と表示されて失敗する問題がありました。

原因は **GitHub API の利用回数の上限**です。この上限は 1 時間あたり 60 回で、
**端末ごとではなく接続元のネットワーク（IP アドレス）ごと**に数えられます。
そのため会社などの共有回線では、他の人の通信で先に使い切られてしまい、
自分が何度も押していなくても 403 になることがありました。

対策として、確認の手順を次のように変えました。

1. まず **Atom フィード**で最新のバージョンだけを調べます。これは上記の上限とは
   **別枠**のため、回数を気にせず使えます。
2. ここで「最新版を使っている」と分かれば、**API を呼ばずに終わります**。
   普段の確認で上限を消費しなくなります。
3. 新しい版が見つかったときだけ API を呼び、ダウンロード先と SHA256 を取得します。
4. その API が失敗しても、ダウンロード先は規則から組み立てられるため**更新を続行**します。

### エラーメッセージの改善
403 のときは、利用者の操作が原因ではないことが伝わる文面に変えました。

> 配布元への問い合わせが、回数の上限に達していました。この上限は同じネットワークを使う人たちで
> 共有されるため、自分が何度も押していなくても起こります。しばらく時間をおくか、
> リリースページから直接ご確認ください。

### バージョンを確認しやすくしました
**トレイメニューの先頭に、現在のバージョンを表示**するようにしました。

```
アタイの貼り付け v1.2.3
─────────────────
✔ Ctrl+B で値貼り付け
✔ 自動起動
─────────────────
更新を確認
─────────────────
終了
```

v1.2.2 ではツールチップに入れていましたが、カーソルを合わせないと見えず、
Windows 11 ではトレイアイコン自体が「^」の中に隠れるため、実質的に確認できませんでした。

### 安全性と応答性
- **タイムアウトを明示**しました（確認 15 秒 / ダウンロード 10 分）。従来は既定値まかせで、
  応答が無い場合に環境によっては最大 90 秒ほど無反応になり得ました。
- **更新ファイルの取得先を GitHub のホストに限定**しました。実行ファイルを取得して
  そのまま実行する処理のため、応答に書かれた URL をそのまま信用しません。

### SHA256 の検証についての訂正
README に「必ず SHA256 で検証します」と書いていましたが、**これは正確ではありませんでした**。
API から `digest` を取得できたときだけ検証しており、取得できない場合（上記の 403 など）は
省略しています。実態に合わせて記述を訂正しました。

省略した場合も通信は HTTPS で行っているため、そこで防げるのは転送中の破損までです。
あわせて、取得先ホストの限定と、先頭 2 バイトが `MZ`（実行ファイルの署名）であることの
確認は省略時も行っています。
