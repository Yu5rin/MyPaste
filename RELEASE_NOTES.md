# アタイの貼り付け v1.1.0

Excel 使用時に **`Ctrl+B` を `Ctrl+Shift+V`（値貼り付け）にリマップ**する Windows 常駐アプリです。
今回はタスクトレイメニューの分かりやすさと、アイコンの視認性を改善しました。

## v1.0.0 からの変更点

### 🎯 メニューを分かりやすく改善
右クリックメニューで、**現在有効な項目に「✔」が付く**ようになりました。

```
✔ Ctrl+B で値貼り付け
✔ 自動起動
─────────────
終了
```

- 文言を変更: 「ON/OFF切替」→ **「Ctrl+B で値貼り付け」**、「自動起動 ON/OFF」→ **「自動起動」**
- 「終了」の前に区切り線を追加
- 起動時の「自動起動」のチェック状態を、実際の設定から反映するように

### 🎨 アイコンの視認性を改善
- **ON**: 黒い「B」に白い縁取りを追加し、暗い背景でも見やすく
- **OFF**: 禁止マークの内側を白背景にして文字をはっきり表示
- `.ico` に 16〜256px の全サイズを内包するよう修正（従来は 16px のみで、拡大表示時に潰れていました）
- 実行ファイル自身のアイコンを禁止マーク付きの絵柄に変更

### 📦 その他
- 実行ファイル名を `Atai-paste.exe` に変更

## 主な機能
- **Excel 限定リマップ**: フォアグラウンドが Excel（`EXCEL.EXE`）のときだけ `Ctrl+B` → `Ctrl+Shift+V`。それ以外では `Ctrl+B` はそのまま通します。
- **自動起動設定**: メニュー「自動起動」でスタートアップ登録／解除。
- **オートリピート抑制**: `Ctrl+B` を押しっぱなしにしても貼り付けは 1 回だけ。
- **安全設計**: 自己入力を除外して無限ループを防止。終了時にフックを確実に解除。
- 単体 EXE・ポータブル・管理者権限不要。

## 動作環境
- Windows 10 / 11（64bit）

## 使い方
1. 下の **Assets** から `Atai-paste.exe` をダウンロード。
2. 任意のフォルダに置いてダブルクリックで起動（インストール不要）。タスクトレイに「B」アイコンが出ます（起動時 ON）。
3. Excel を最前面にして `Ctrl+B` → 値貼り付けが実行されます。
4. トレイアイコンを右クリック →「Ctrl+B で値貼り付け」「自動起動」「終了」。現在有効な項目には「✔」が付きます。

## 既知の制限
- ON/OFF は右クリックメニューから行います（左クリックのトグルは採用ライブラリ `tray-item` の制約で非対応）。
- 他ソフトとのキー競合は対象外です。

## 注意
- 本 EXE はコード署名していないため、初回起動時に Windows SmartScreen の警告が出る場合があります。「詳細情報」→「実行」で起動できます。
- アイコンが以前のまま表示される場合は Windows のアイコンキャッシュが原因です。`ie4uinit.exe -show` を実行するか、サインインし直してください。
