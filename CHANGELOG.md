# 変更履歴

本プロジェクトの主な変更点をまとめます。バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [1.2.3] - 2026-09-11

### 修正
- **自動更新が HTTP 403 で失敗する問題を修正**。原因は GitHub API の未認証
  レート制限（1 時間 60 回・IP アドレス単位）で、端末ごとではなく共有回線
  全体で数えられるため、利用者が何度も押していなくても起こり得た。
  レート制限とは別枠の Atom フィード（`releases.atom`）で「新しい版がある
  か」をまず確認し、API を呼ぶのは新しい版が見つかったとき（添付ファイルの
  詳細・SHA256 が要るとき）だけにするよう変更。呼ぶ頻度が大きく減るため、
  上限に当たる見込みはほぼ無くなる。
- 上記の変更に伴い、その API 呼び出し自体が失敗しても（403 を含む）、
  Atom で新しい版が分かっていればダウンロード URL を規則から組み立てて
  更新を続行するようにした。引き換えに SHA256 は分からないため、その場合
  だけ検証を省略する（ログに残す）。README の「必ず SHA256 で検証する」
  という記述は誤りだったため訂正した。
- 「更新を確認できませんでした」ダイアログが、HTTP 403（レート制限）の
  ときも他の失敗と同じ文言だったため、利用者が自分の操作が原因だと誤解し
  得た。403 のときは「配布元への問い合わせが、回数の上限に達していました」
  という趣旨の案内に変えた（`http::HttpError` でステータスコードを判別
  できるようにし、`update::CheckError` で分岐）。

### 変更
- 更新の確認・ダウンロードに `WinHttpSetTimeouts` でタイムアウトを明示
  するようにした（確認 15 秒・ダウンロード 10 分）。従来は既定値まかせで、
  応答が無い場合に環境によっては最大 90 秒ほど無反応になり得た。
- 更新ファイルの取得先ホストを `github.com` / `api.github.com` /
  `objects.githubusercontent.com` / `*.githubusercontent.com`（すべて
  HTTPS）に限定するようにした。EXE を取得してそのまま実行する処理のため、
  応答に書かれた URL をそのまま信用しない。

### 変更
- **トレイメニューの先頭に現在のバージョンを表示**するようにした。
  v1.2.2 でツールチップに入れたが、カーソルを合わせないと見えないうえ、
  Windows 11 ではトレイアイコン自体がオーバーフローに隠れるため、実質的に
  確認できなかった。メニューを開けば必ず目に入る位置に置く（`add_label` に
  よる選択できない項目なので、誤って押される心配はない）。

### 内部
- 更新確認の判断ロジック（バージョン比較、Atom フィードからのタグ抽出、
  API URL からの Atom URL 組み立て、ダウンロード URL 組み立て、許可ホスト
  判定）を Win32 API に依存しない `src/update_logic.rs` に切り出した。
  `tray-item` が Windows 専用のためクレート全体の `cargo test` はそのままでは
  動かないが、このファイルは単体で Linux ネイティブの `cargo test` でも
  検証できる。

## [1.2.2] - 2026-08-22

UX と品質の総点検で見つかった問題への対応。

### 修正
- **更新処理の多重実行を防止**。起動時の自動確認とメニューからの手動確認が
  同時に走ると、確認ダイアログが二重に出て、両方で「はい」を押した場合に
  同じ一時ファイルへの同時書き込みや EXE の二重入れ替えが起きる問題があった。
  実行中フラグ（`UPDATE_RUNNING`）を追加し、後からの要求は無視する
  （手動操作の場合はその旨をダイアログで知らせる）。
- **実行ファイルの入れ替え手順を見直し、中断時に起動不能になる窓を縮小**。
  従来は「現在の EXE をリネーム → 新しい EXE をコピーで配置」の順で、コピーの
  途中に強制終了・電源断が起きると `Atai-paste.exe` が消えたり壊れたりし得た。
  新しい EXE を先に `Atai-paste.exe.new` としてコピーしておき、入れ替え自体は
  リネーム 2 回（`exe → .old`、`.new → exe`）だけで行うよう変更。あわせて、
  起動時のクリーンアップで「`.exe` が無く `.old` だけが残っている」場合に
  `.old` を `.exe` へ復元するようにした。
- 終了処理（トレイメニューの「終了」）が、更新の入れ替え中であれば完了を
  （最大 60 秒まで）待ってから行うようにした。従来は更新スレッドが途中で
  打ち切られ、実行ファイルが失われる可能性があった。
- キーボードフックの設置失敗、タスクトレイの初期化失敗のいずれも、
  従来はログに残すだけで（本番ビルドはログを出力しないため）利用者には
  無言で終了しているように見えていた。MessageBox で知らせるようにした。
- `settings.json` が **UTF-8 の BOM 付き**で保存されていると解析に失敗し、
  利用者の設定が黙って無視されていたのを修正（BOM を読み飛ばすようにした）。
- `settings.json` の読み込みが失敗した際、原因を問わず既定値で**上書き保存**
  していたため、UTF-16 保存などで利用者の編集内容が失われる恐れがあった。
  ファイルが**存在しない**場合だけ既定値を書き出すようにした。
- HTTP 応答が `Content-Length` の途中で切断された場合に正常終了扱いに
  なっていたのを修正（受信バイト数を突き合わせてエラーにする）。
- 更新ファイルのダウンロード後、書き出し前に**先頭 2 バイトが `MZ`**
  （PE 実行ファイルの署名）であることを確認するようにした。SHA256 の
  `digest` が無いリリースでも最低限の妥当性検査になる。

### 変更
- **ON アイコンの白い縁取りを太くした**（256px 基準で 12 → 20）。従来は
  16px に縮小すると縁取りが 1px 未満になり、既定が黒い Windows 11 の
  タスクバーで黒い「B」が背景に溶けてほとんど見えなかった。
- トレイアイコンのツールチップに現在のバージョン（例:
  `アタイの貼り付け v1.2.2`）を表示するようにした。更新を確認しなくても
  バージョンを確認できる。
- SHA256 検証に失敗した際、64 桁の hex をダイアログに出していたのをやめ、
  「壊れている可能性があります」という平易な文言に変更（hex はログにのみ残す）。
- エラーダイアログの構成を統一。先頭に日本語の平易な一文を置き、
  serde / WinHTTP などの詳細なメッセージはその下に「詳細:」として添えるように
  した（詳細自体は原因の切り分けに使えるため残す）。
- 更新の確認・取得に失敗した場合も、適用失敗時と同様にリリースページへの
  導線をダイアログの文面に含めるようにした。

### ドキュメント
- README の「実行中の EXE を入れ替える仕組み」の記述を訂正。
  「壊れた状態では残らない」という誤った断定をやめ、何が保証され何が
  保証されないか（電源断などでは窓が残ること）を明記した。
- README・RELEASE_NOTES の「動作環境」に、`Ctrl+Shift+V`（値貼り付け）が
  **Microsoft 365 バージョン 2308（2023年8月）以降**のショートカットである
  ことを明記。Excel 2019 / 2021 などの永続版や古い Microsoft 365 では
  反応せず、`Ctrl+B`（太字）も効かなくなる旨と、その場合の対処
  （OFF にするか終了する）を追記した。

## [1.2.1] - 2026-08-22

### 変更
- **起動のたびに更新を確認するようにした**（従来は 1 日 1 回まで）。
- 設定 `check_interval_hours` を追加し、確認の間隔を指定できるようにした。
  既定は `0`（毎回）。`24` にすると従来どおり 1 日 1 回までになる。

### 修正
- 更新ファイルの書き出しが途中で失敗したときに、中途半端な一時ファイルが
  残る場合があったのを修正。
- 強制終了などで一時フォルダに残った更新ファイルを、起動時に掃除するようにした。
  対象は `Atai-paste-<バージョン>.exe` という名前で、かつ 1 時間以上前のものだけ
  （更新中の別インスタンスのファイルや、無関係なファイルには触れない）。
- `build.rs` が Windows 以外のホストからのクロスビルドで、リソースコンパイラが
  無い場合にビルドを失敗させていた問題を修正。型チェック目的のビルドでは
  リソースの埋め込みを省略する。

## [1.2.0] - 2026-08-22

### 追加
- **自動更新**を搭載。GitHub の最新リリースを調べ、新しい版があれば確認のうえ
  実行ファイルを入れ替える。
  - 通信するのは「メニューから更新を確認したとき」と「起動時（1 日 1 回まで）」の
    2 つの場合だけ。通信は別スレッドで行い、起動やキー操作を妨げない。
  - ダウンロードしたファイルは **SHA256 で検証**する（GitHub がリリース資産に
    付与する `digest` と突き合わせる）。進捗はトレイのツールチップに表示。
  - 実行中の EXE はリネーム方式で入れ替え、失敗時は巻き戻す（**ロールバック**）。
  - `Program Files` 配下など書き込めない場所では自動更新せず、リリースページを
    開いて手動での入れ替えを案内する。
- トレイメニューに「更新を確認」を追加。
- 設定ファイル `settings.json` を追加。**通信先の URL を利用者が確認・変更できる**。
  `check_on_startup` を `false` にすると起動時の確認をやめ、手動のみにできる。
- コミット名義の方針とプロジェクトガイドをまとめた `CLAUDE.md` を追加。

### 技術的な補足
- HTTPS 通信には Windows 標準の WinHTTP を使用。証明書の検証を OS に任せられ、
  C やアセンブリのビルドを伴う TLS クレートを避けられる。
- バージョン比較は必ず数値として行う（文字列比較では `1.0.10` < `1.0.9` と誤判定するため）。

## [1.1.0] - 2026-07-28

### 変更
- タスクトレイの右クリックメニューを分かりやすく改善。
  - 有効な項目の先頭に「✔」を表示し、現在の状態が一目で分かるように。
  - 文言変更: 「ON/OFF切替」→「Ctrl+B で値貼り付け」、「自動起動 ON/OFF」→「自動起動」。
  - 「終了」の前に区切り線を追加。
  - 起動時の「自動起動」のチェック状態を実際の設定から反映。
- 実行ファイル名を `atai-paste.exe` から `Atai-paste.exe` に変更。
- アイコンの配色を調整。
  - ON: 黒「B」に白い縁取りを追加し、暗い背景でも視認しやすく。
  - OFF: 禁止マークの内側を白背景にして文字を見やすく。
- 実行ファイル自身のアイコンを禁止マーク付きの絵柄に変更。

### 修正
- `.ico` に 16×16 の 1 サイズしか含まれておらず、拡大表示で潰れていた問題を修正
  （16〜256px の全サイズを内包するように変更）。
- リソースの並び順により、EXE のアイコンが意図せず OFF アイコンになっていた問題を修正。

## [1.0.0] - 2026-07-21

### 追加（初回リリース）
- Excel（`EXCEL.EXE`）が最前面のときのみ `Ctrl+B` を `Ctrl+Shift+V`（値貼り付け）にリマップ。
- タスクトレイ常駐＋右クリックメニュー（ON/OFF切替・自動起動・終了）。
- ON/OFF でアイコン切替（黒「B」／黒「B」＋赤禁止マーク）。
- 自動起動設定（スタートアップフォルダへの `.lnk` 作成・削除）。
- オートリピート抑制（押しっぱなしでも貼り付けは 1 回）。
- 自己入力の除外（`dwExtraInfo` 署名＋`LLKHF_INJECTED`）で無限ループを防止。
- 単体 EXE・ポータブル・管理者権限不要（`asInvoker` マニフェスト）。
- 開発時のみ `log.txt` を出力（本番ビルドでは無効）。

### 既知の制限
- ON/OFF 切替は右クリックメニューから行う（左クリックによるトグルは `tray-item` の制約により非対応）。
- 他ソフトとのキー競合は対象外。

[1.2.3]: https://github.com/Yu5rin/MyPaste/releases/tag/v1.2.3
[1.2.2]: https://github.com/Yu5rin/MyPaste/releases/tag/v1.2.2
[1.2.1]: https://github.com/Yu5rin/MyPaste/releases/tag/v1.2.1
[1.2.0]: https://github.com/Yu5rin/MyPaste/releases/tag/v1.2.0
[1.1.0]: https://github.com/Yu5rin/MyPaste/releases/tag/v1.1.0
[1.0.0]: https://github.com/Yu5rin/MyPaste/releases/tag/v1.0.0
